import { Config } from "../models/config.model";

export const configMock: Config = {
    api: {
    wsenemy: {
      base: "http://localhost:test/",
      uri: {
        enemies: "api/enemies"
      }
    }
  }}